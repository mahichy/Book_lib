<% if @photos.count > 0 %>
	<br><br>

	<div class="row">
		<% @photos.each do |ph|%>
			<div class="col-md-4">
				<div class="panel panel-default">
					<div class="panel-heading preview">
						<%= image_tag ph.image.url()%>
					</div>
				</div>
			</div>
		<% end -%>
	</div>

<% end -%>